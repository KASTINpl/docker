composer:
    build: ./composer/
    dockerfile: ./Dockerfile
    container_name: composer
    log_driver: syslog
    environment:
      - TERM=xterm-256color
    volumes:
      - ~/.ssh:/home/php/.ssh
      - ~/.docker-composer/:/home/php/.composer
      - ~/Projekty/:/opt
